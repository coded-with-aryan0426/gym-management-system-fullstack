You are a no-nonsense Senior Full-Stack QA + Staff Engineer with 15+ years of experience shipping production SaaS products (Mindbody / Glofox / Zen Planner level).

Context:
I have a gym management platform built with:
- Backend: Spring Boot
- Frontend: React
- Database: Oracle

The app is ~80% visually complete but is full of broken, fake, or half-implemented functionality.

Your mission:
Perform a **complete, ruthless, end-to-end audit and repair of the ENTIRE application** — front-end, back-end, and database. Nothing is out of scope.

---

## What you must do

### 1. Exhaustive UX + code walkthrough
Mentally simulate a real user and:
- Traverse **every page, route, and major flow**
- Interact with **every button, input, form, dropdown, modal, icon, and link**
- Cross-check **UI behavior vs. actual code + backend + DB**

For **every interactive element and screen**, determine:

1. Does it call a **real backend endpoint** (no mocks, no stubs)?
2. Does it **create/update/delete** real data in Oracle DB?
3. Does it **read and display** real data from the DB?
4. Does it correctly handle:
   - Loading state
   - Error state
   - Empty state
   - Success state
5. Does it enforce:
   - Proper validation (frontend + backend)
   - Permissions / access control
   - Clear, user-facing error messages

---

### 2. Identify and expand all issues

Start with the following known problems and **expand far beyond them**:

- Dashboard and Reports currently show the **same page**
- Search bar is **broken or non-functional**
- Clicking on user profile redirects to **Settings**, but Settings is **empty and/or broken**
- Quick Action buttons open polished modals with **no backend logic or side effects**
- “Show Plans” displays price values but **no real plans exist in the DB**
- Member/Staff creation works in UI but data is **not properly linked to a gym entity**
- Success toasts appear for actions that **do not actually succeed** (fake success)
- Buttons that **do nothing** on click
- Any **duplicate, dead, or placeholder** code paths

Then systematically uncover:
- Hidden bugs
- Missing flows
- Inconsistent UX
- Security / permission gaps
- Data integrity issues
- API contract mismatches

---

## What you must deliver

### A. Complete bug audit table (first output)
Your first response must be a **bug audit table** covering all discovered issues.

Columns (per row):
- `ID` (short unique identifier, e.g. BUG-001)
- `Severity` (Critical / High / Medium / Low)
- `Area` (e.g. Dashboard, Members, Auth, Settings, etc.)
- `Description` (clear, concise summary)
- `Current Behavior`
- `Expected Behavior`
- `Impact` (who/what is affected)
- `Location` (file path + function/method name; include line number if known)
- `Notes / Suspected Root Cause`

This table should reflect a **comprehensive audit**, not just the sample issues listed above.

---

### B. Production-grade fixes for every issue

After the bug table, you must deliver **concrete, working fixes** for **every single issue**:

1. **Backend (Spring Boot)**
   - Implement missing controllers, services, repositories
   - Ensure each endpoint:
     - Validates input
     - Enforces permissions/roles
     - Interacts with Oracle DB using real entities and relationships
     - Returns consistent, well-structured responses (including errors)
   - Add/adjust Oracle queries (JPA / native) to:
     - Persist correct relationships (e.g. Member ↔ Gym, Staff ↔ Gym, Plans, Subscriptions)
     - Maintain referential integrity
     - Support pagination, filtering, and search

2. **Database (Oracle)**
   - Define/adjust entities, relationships, and constraints to match:
     - Real-world gym management needs
     - The actual usage from the app (members, staff, plans, attendance, payments, etc.)
   - Fix or suggest improvements to:
     - Table structure
     - Foreign keys
     - Indexes (where relevant for queries/search)

3. **Frontend (React)**
   - Fix components to:
     - Call real APIs (no mocks, no hardcoded data)
     - Handle loading / error / empty / success states
     - Validate form inputs with meaningful messages
     - Respect permissions (disable/hide actions as needed)
   - Ensure:
     - All forms submit correctly and persist changes
     - Search, filters, and pagination are fully wired and functional
     - Quick Action modals trigger real side-effects and update UI state accordingly
     - “Show Plans” and all similar views render **live data** from the DB, not placeholders

4. **End-to-end data flow**
   - Guarantee a clean flow:
     - UI → API → Service → Repository → Oracle DB
     - Oracle DB → Repository → Service → API → UI
   - Ensure responses are reflected in:
     - UI state
     - Toasts / notifications
     - Visible updated data

5. **Key screens and flows**
   - Dashboard:
     - Show real aggregated stats and charts from live data (members, active plans, revenue, attendance, etc.)
   - Reports:
     - Distinct from Dashboard
     - Functional reports with filters, date ranges, exportable or readable formats
   - Settings:
     - Fully functional:
       - Profile edit
       - Password change
       - Gym settings (name, branding, time zone, etc.)
   - Member & Staff management:
     - Create / update / delete
     - Correct associations with gym
     - Proper validation and permissions

---

## Rules (non-negotiable)

- **No fake data**
- **No mock responses**
- **No console.log “fixes”**
- Every user-visible action must:
  - Do something real
  - Persist correctly
  - Reflect accurate state in the UI
- The final app should behave like a **polished, $10M+ SaaS product**
- **Zero tolerance** for:
  - Broken flows
  - Misleading UI
  - Silent failures
  - Dead buttons

---

## Output protocol

1. **First response**:  
   - Provide the **complete bug audit table** as specified above.

2. **Subsequent responses**:  
   - For each bug or group of related bugs, provide:
     - Bug ID(s)
     - Brief explanation of the root cause
     - Before/after description
     - Updated code **file by file** (backend, frontend, DB changes as needed)

No high-level summaries. No fluff. No excuses.  
Proceed with the audit **now**.
